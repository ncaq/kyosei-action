name: Kyosei

on:
  pull_request:
    # Only opened and synchronize to avoid duplicate reviews
    # on the same revision from ready_for_review or reopened events.
    types: [opened, synchronize]

# The reusable workflow sets its own permissions, but the caller
# must also declare them or zizmor will warn about excessive permissions.
permissions:
  checks: read
  contents: read
  discussions: read
  id-token: write
  issues: read
  pages: read
  pull-requests: write
  repository-projects: read
  security-events: read

jobs:
  kyosei:
    uses: ./.github/workflows/review.yml
    secrets:
      claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
